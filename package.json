{
  "name": "outlook-calendar-scroll",
  "version": "1.1",
  "description": "A userscript to enhance scrolling in PWA Outlook calendar",
  "devDependencies": {
    "@types/node": "^24.2.0",
    "npm-run-all": "^4.1.5",
    "serve": "^14.2.4",
    "tsc": "^2.0.4",
    "vite": "^7.1.0"
  },
  "scripts": {
    "build:watch": "vite build --watch",
    "build": "vite build",
    "serve": "serve dist",
    "dev": "npm-run-all --parallel build:watch serve"
  }
}
